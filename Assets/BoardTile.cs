using UnityEngine;
using TMPro;
public class BoardTile : MonoBehaviour
{
    public TMP_Text boardTileCaptionText;

    public int row;
    public int col;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
